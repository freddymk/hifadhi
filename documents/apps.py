from django.apps import AppConfig


class DocumentsConfig(AppConfig):
    name = 'documents'
    
# Document management:
# Handles student documents such as MOAs, receipts, results and other uploads.
